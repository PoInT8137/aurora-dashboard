# Тесты и скрипты проверки

Дашборду для работы не нужны, в страницу не входят. Запускаются на Node.js 24 (нужен встроенный SQLite для тестов сервера, остальному хватает 18+), зависимостей нет.

## Тесты

```bash
node --test tools/*.test.mjs                 # 385 тестов: сайт, настройки, переводы, Луна, клиент push, service worker
cd worker && npm test                        # 115 тестов сервера уведомлений
```

На GitHub оба набора запускаются сами на каждый пуш в `main` и на пулреквесты (`.github/workflows/tests.yml`, Node.js 24, без установки зависимостей). Тесты не зависят от часового пояса машины — проверено в UTC, Москве и Нью-Йорке.

| Файл | Что проверяет |
|---|---|
| `parity.test.mjs` | **сайт и сервер уведомлений согласны, высокий ли шанс:** ~970 тыс. сравнений, 7 точек, реальная высота Солнца весь год (полярный день, ночь, сумерки); разбор ответов Open-Meteo и NOAA; одна и та же модель погоды |
| `push-client.test.mjs` | подписка браузера: разрешение, ключ, регистрация на сервере, откат при отказе сервера, потерянная подписка, отозванное разрешение, зависшая подписка |
| `sw-push.test.mjs` | service worker: показ push, запасное уведомление при недоступном сервере, приложение в фокусе, нажатие на уведомление, полнота оболочки |
| `moon.test.mjs` | **Луна против Морской обсерватории США:** 50 фаз за 2026 год, восходы и заходы в двух точках, освещённость, полярные случаи (Луна не заходит, не восходит, видна 17 минут), пороги помехи на границах. Эталон — `fixtures/usno-moon-2026.json` |
| `moon-ui.test.mjs` | Луна на странице: фактор и пояснение в вердикте, строка в окне наблюдения, чип на вкладке «Куда ехать»; уровень вердикта от Луны не зависит |
| `i18n.test.mjs` | **переводы:** те же ключи и подстановки во всех словарях, нет кириллицы в английском и китайском, каждый ключ из `app.js` и `index.html` существует, а в словаре нет мёртвых; определение языка (адрес, выбор, браузер, запасной английский); формы множественного числа; страница на каждом языке (время, возраст данных, вердикт, «Куда ехать», ошибки загрузки); нажатие на переключатель; уровень вердикта не зависит от языка |
| `settings.test.mjs` | **настройки:** значения по умолчанию, испорченное и недоступное хранилище, недопустимые имена и значения (включая `__proto__`), часовой пояс, 12/24 часа, мили и °F с правильными словами на трёх языках, автообновление (таймер, возврат на вкладку), перерисовка по нажатию, сброс, перенос уведомлений во вкладку «Настройки» и старые ссылки `#notify` |
| `prefs.test.mjs` | **оформление и поведение:** тема (тёмная, светлая, «как в системе», «ночное зрение»: только красные цвета, контраст, кнопка в шапке с возвратом прежней темы), встроенный скрипт до первой отрисовки даёт те же атрибуты, что код страницы; светлая тема переопределяет все переменные; контраст ≥ 4,5:1 на фоне и карточке в обеих темах; в правилах нет цветов-литералов мимо переменных; размер текста; стартовая вкладка; тихие часы по поясу, через полночь и на границах; уведомление страницы в тихие часы; предпочтения для сервера |
| `solar-wind.test.mjs` | **солнечный ветер:** уровни и их границы, «устойчиво» против короткого провала, среднее за 5 минут, только основной поток `active`, мусор и записи из будущего, обрезка до двух часов, молчание спутников больше получаса, время в пути от L1; карточка на трёх языках, график и его шкала, отказ источника и кэш; подсказка в вердикте и когда её быть не должно |
| `ovation.test.mjs` | **модель OVATION:** клетка точки и поле зрения с его границами, переход долготы через 0°, полюс, все точки за проход, устаревшее наблюдение, мусор в сетке; карточка на двух языках, пороги подписей, смена точки без запроса, запрос не чаще раза в 15 минут, в кэше нет сетки, отказ сети |
| `weather.test.mjs` | **погода на месте:** разбор ответа Open-Meteo, км/ч → м/с, условия и их приоритет с границами (видимость 1 км, порывы 15 м/с), направление ветра через 360°, порывы и штиль, мили и °F без двойного округления, три языка, старый кэш без погоды, туман в вердикте, поля в запросе, отказ сети |
| `outlook.test.mjs` | **прогноз на 27 дней:** разбор таблицы NOAA и мусора, дни с сегодняшнего, уровни по порогам точки, светлые ночи и полнолуние, лучшие даты (подряд, пропуск в таблице разрывает отрезок, сильнейшие по порядку дат, сильная буря при Луне), карточка на трёх языках, смена точки без запроса, частота запросов, одновременные открытия, отказ сети и срок годности сохранённого |
| `guide.test.mjs` | **гид:** порядок вкладок в разметке и в коде, связь панели с кнопкой, пять разделов с текстом на трёх языках, русский текст в разметке совпадает со словарём, советов по одежде нет |
| `tabs.test.mjs` | **панель вкладок:** выбранная вкладка доезжает до середины, у края не уходит в минус, на широком экране не прокручивается, стили горизонтальной прокрутки на телефоне |
| `history.test.mjs` | **прошлые ночи и точность прогноза:** итог сверки по-русски, «статистика копится» до пяти ночей, без сервера — без карточки; разбор измеренного Kp, только завершившиеся и целиком попавшие в данные ночи, оценка по правилам окна наблюдения (в том числе сумерки), белые ночи, два запроса и строки с оценкой, кэш и ошибка, данные своей точки, разметка |
| `map.test.mjs` | **карта:** проекция и углы рамки, все точки внутри карты, контуры и размер `map.js`; маркеры на своих местах и на языке страницы, цвет по шансу, подписи для скринридера, разведённые подписи Кировска и Апатитов, выбор точки и переход к её условиям, состояние без данных и с сохранёнными данными, загрузка при открытии вкладки |
| `night-chart.test.mjs` | **график ночи:** столбец на час, подписи рядов, рамка окна и сумерки, высота столбиков облаков и Kp, Луна по часам и в середине часа (по расчёту восходов и заходов), подробности и подпись для скринридера, выбор мышью и стрелками, сброс выбора, шаг подписей времени, английский, полярный день |
| `share.test.mjs` | **«Поделиться»:** ссылка на языке и её разбор, QR-матрица (размер, три поисковых узора), SVG с тихой зоной и безопасной подписью, язык по умолчанию и смена в окне, окно без `showModal`, копирование (успех, отказ, нет буфера), системное меню и отмена, PNG для печати, отказ загрузки библиотеки, библиотека в оболочке и подгружается по требованию |
| `clouds.test.mjs` | **облака на карте:** сетка узлов по рамке схемы, один запрос с той же моделью, разбор ответа (модель перекрытия, пропуски, чужие часы, длина), отбрасывание прошедших часов, билинейная заливка и края, непрозрачность, подпись часа на языке страницы, загрузка и рисование, ползунок, облачность у выбранной точки, выключение с запоминанием, не чаще раза в час, отказ сети и кэш, разметка и береговые линии поверх |
| `places.test.mjs` | **свои места:** геомагнитная широта совпадает с точками области, разбор координат (запятая, пара в одном поле), район, расстояние и пороги Kp, хранение (мусор, повторы, подмена точки области, лимит, имя как текст), добавление и удаление, общий запрос облачности и «Куда ехать» без подмены удалённых, уведомления по ближайшей точке, строки без засветки, список с группой и пунктом «Добавить…», окно (ошибка, сохранение и выбор), «Где я сейчас», выбор на карте и обратная проекция, подписи на карте, разметка |
| `reports.test.mjs` | **очевидцы на странице:** сводка (подробно у своей точки, число людей по-русски, остальные списком, тон карточки), отметка (выбор силы, запрос, благодарность, пауза 30 минут), днём нельзя, своё место — у ближайшей точки, ошибки сервера, сбой сводки, без сервера — без карточки и запросов, кэш на 2 минуты, разметка |
| `shortcuts.test.mjs` | **ярлыки приложения:** три ярлыка внутри области приложения, короткие подписи, значки существуют, ведут на существующие вкладки, `?theme=night` включает ночное зрение с запоминанием прежней темы и убирается из адреса, встроенный скрипт применяет его до отрисовки, манифест в оболочке |
| `seo.test.mjs` | **поиск и превью:** адрес совпадает с CNAME, заголовок и описание, canonical и hreflang для трёх языков, Open Graph и Twitter с картинкой 1200×630 из репозитория, JSON-LD, robots.txt и sitemap.xml с теми же адресами, заголовок вкладки переведён, canonical при смене языка |
| `splash.test.mjs` | **заставка первого визита:** один раз на устройство, «уменьшить движение» и недоступное хранилище — без заставки, лёгкое интро в первый визит отключено, язык по правилам сайта, тексты совпадают со словарями, нарастание и кадр сияния (столбики, сложение света, границы холста), звёзды, пропуск кнопкой, Esc, нажатием и сворачиванием, завершение по таймеру и без кадров после, стили: слой поверх, скрытая страница, `safe-area`, CSS-страховка для «уменьшить движение» |
| `intro.test.mjs` | **анимация запуска:** первый запуск за сеанс ставит и снимает класс, повтор — без анимации, «уменьшить движение» и недоступное хранилище — без анимации и без ошибок, порядок встроенных скриптов, слой скрыт от скринридеров и нажатий, все анимации укладываются в 2,6 с, CSS-страховка для «уменьшить движение» |
| `heartbeat.test.mjs` | **пульс сервера на странице:** разбор `/health` (старый сервер, мусор, недоступен), строка диагностики — свежий пульс, пропуски из-за данных, больше 30 минут без проверок и граница, сервер без пульса; строка в карточке на двух языках |
| `structure.test.mjs` | **разбиение кода и разметка:** все id в index.html уникальны; каждый файл из `js/` подключён и ничего лишнего, порядок подключения, строгий режим у каждой части, `app.js` — только точка входа, все части в оболочке офлайна |
| `app-ui.test.mjs` | тексты ошибок (включая настоящие `DOMException`), кнопки не залипают при сбоях |
| `app-sandbox.mjs` | не тест: песочница, запускающая приложение в Node без браузера; сама подставляет части из `js/` в порядке `index.html`, `appSource()` отдаёт весь код для проверок исходника |

`app.js` запускается в песочнице без браузера: DOM заменён заглушкой, время и сеть подменяются. Так расчёты проверяются на сотнях тысяч комбинаций входных данных.

## Одноразовые скрипты

Ими проверялись решения, описанные в основном README.

```bash
node tools/build-map.mjs <admin_1.geojson> <lakes.geojson>   # собрать map.js для вкладки «Карта»
node tools/compare-models.js      # снимок облачности по 12 моделям Open-Meteo
node tools/check-consistency.js   # согласованность ярусов и суммы за 48 часов
node tools/compare-formulas.js    # взвешенная сумма против модели перекрытия
```

**build-map.mjs** собирает `map.js` — схему для вкладки «Карта» — из файлов Natural Earth `ne_10m_admin_1_states_provinces.geojson` и `ne_10m_lakes.geojson` (github.com/nvkelso/natural-earth-vector, общественное достояние): проецирует контуры, отсекает рамкой и упрощает (Дуглас — Пекер). Нужен только при смене рамки; в репозитории лежит готовый `map.js` (≈12 КБ).

**compare-models.js** запрашивает у каждой модели текущую облачность и три яруса и проверяет неравенство `max(ярусы) ≤ всего ≤ сумма(ярусы)`, обязательное при любом допущении о перекрытии слоёв. Этим скриптом обнаружено, что `best_match` для Мурманска выбирает MET Norway и что у неё это неравенство нарушается.

**check-consistency.js** делает то же самое по всему 48-часовому почасовому ряду и считает долю часов с нарушением — чтобы отличить разовый сбой от систематического.

**compare-formulas.js** сравнивает две формулы сведения ярусов в одно число на крайних и смешанных случаях и прогоняет их по всему пространству значений с шагом 5 %.

Результаты и выводы — в разделе [«Почему выбрана эта модель прогноза»](../README.md#почему-выбрана-эта-модель-прогноза).
