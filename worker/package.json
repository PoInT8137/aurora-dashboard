{
  "name": "aurora-push",
  "private": true,
  "type": "module",
  "description": "Сервер уведомлений о северном сиянии: проверка условий по расписанию и Web Push",
  "scripts": {
    "test": "node --test test/push.test.mjs test/api.test.mjs test/check.test.mjs",
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
